@isTest

private class newContactsTest {

    static testMethod void makeContacts() {

        // this covers all menthods
        // so simple
        newContacts newContactsClass = new newContacts();
        newContactsClass.makeDoubleContacts();

    }
}